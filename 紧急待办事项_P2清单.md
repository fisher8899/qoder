# 紧急待办事项（P2及后续）

## 1. 权限与数据范围残留问题
- 前后端权限仍是双轨模型，前端静态路由、动态菜单、请求头职责上下文三套逻辑并存。
- 数据范围过滤实现仍不统一，部分查询按 `org_id`，部分按 `unit_id` 回退，仍有串数风险。

## 2. 业务假数据与场景收口问题
- `BizExamGroupServiceImpl#getProgress()` 仍含占位状态，返回结果不可信。
- `IndicatorSet.vue` 弹框内考核组来源与主列表过滤条件不一致，仍可能混入非 `INDICATOR_SET`` 场景数据。

## 3. 配置与编码治理
- `application.yml` 与 `application.properties` 仍是双份配置，虽然本次保持同步，但后续应合并为单一配置源。
- 代码库存在大面积中文乱码，用户提示、日志、注释可读性差，需要单独清理。

## 4. Flyway 基线完善
- 当前已把历史 schema 修正纳管到 Flyway，但 `db/schema.sql` / `db/data.sql` 仍未整理为标准 `V1` 基线迁移。
- 新环境一键初始化链路仍不完整，后续应补齐标准基线脚本并清理历史散落 SQL。
