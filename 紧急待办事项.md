# 紧急待办事项

以下问题本轮未处理，需单独排期：

## P2

1. 考核进度接口存在占位假数据  
   - 文件：`assessment-backend/src/main/java/com/ccerphr/assessment/service/impl/BizExamGroupServiceImpl.java`
   - 问题：进度状态存在硬编码，返回结果不可信
   - 建议：按真实业务表聚合，统一状态机

2. 指标设定页面二次加载考核组时场景过滤不一致  
   - 文件：`assessment-frontend/src/views/dept/IndicatorSet.vue`
   - 问题：主列表按 `INDICATOR_SET` 过滤，弹框列表未严格复用同一过滤条件
   - 建议：页面内部统一使用同一场景过滤器

3. 职责切换状态仍主要依赖前端本地缓存  
   - 文件：`assessment-frontend/src/stores/user.ts`、`assessment-frontend/src/layouts/MainLayout.vue`
   - 问题：刷新、切用户、切职责后仍可能出现上下文不一致
   - 建议：改为后端确认当前职责快照，前端只消费结果

4. 数据范围过滤模型仍未完全标准化  
   - 文件：`assessment-backend/src/main/java/com/ccerphr/assessment/util/DataScopeFilter.java`
   - 问题：不同业务对象可能按 `unitId` 或 `orgId` 走不同分支，后续仍有串数风险
   - 建议：建立统一隔离键规则矩阵，收口到仓储层

5. 全局异常处理仍缺少完整审计日志  
   - 文件：`assessment-backend/src/main/java/com/ccerphr/assessment/common/GlobalExceptionHandler.java`
   - 问题：虽然本轮修了主链路，但全局异常仍可能把真实根因抹平
   - 建议：补全 requestId、userId、职责、scope、uri、参数摘要、异常栈

## P3

1. 配置重复  
   - 文件：`assessment-backend/src/main/resources/application.yml`、`assessment-backend/src/main/resources/application.properties`
   - 建议：保留一种格式，另一份删除

2. 中文乱码和编码污染  
   - 范围：前后端多文件
   - 建议：统一 UTF-8，优先修复用户可见提示、菜单、异常消息

3. 数据库迁移体系仍需正式落地  
   - 问题：当前仍有手工 SQL 与结构漂移风险
   - 建议：引入 Flyway 或 Liquibase，建立版本化迁移
